package org.huntercore.api.gui;

/** Visual capability selected for a GUI render. */
public enum HunterGuiTheme {
    /** Use only components that remain legible and actionable in a stock client. */
    VANILLA,
    /** A plugin has established that its richer resource-pack treatment is available. */
    RESOURCE_PACK
}
